{
    "version": 2,
    "builds": [
      { "src": "gulpfile.js", "use": "@vercel/static-build", "config": { "distDir": "theme" } }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" },
      { "src": "/(.*)", "dest": "/theme/$1" }
    ]
  }
  